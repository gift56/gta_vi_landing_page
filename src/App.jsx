import gsap from "gsap";
import { ScrollTrigger } from "gsap/all";

gsap.registerPlugin(ScrollTrigger);

const App = () => {
  return <main>
    <div className="text-3xl-font-bol">Hello</div>
  </main>;
};

export default App;
